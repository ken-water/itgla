{
  "name": "itgla-website",
  "private": true,
  "version": "0.2.1",
  "scripts": {
    "test": "npm run test:language && playwright test",
    "test:language": "node ../scripts/check-english-ui.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
